import React, { useState } from "react";
import { WORK_TIME } from "../constants";

const TimerNew = () => {
  const [count, setCount] = useState(WORK_TIME);

  return <div>TimerNew</div>;
};

export default TimerNew;
